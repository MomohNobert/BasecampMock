<%= form_with(model: project_thread, local: true) do |form| %>
  <% if project_thread.errors.any? %>
    <div class="error-message" id="error_explanation">
      <h2><%= pluralize(project_thread.errors.count, "error") %> prohibited this project_thread from being saved:</h2>

      <ul>
        <% project_thread.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :project_id, :value => params[:project_id], :type => "hidden" %>
  </div>

  <div class="form-container-title">
    <%= form.label :thread_title, 'Thread Title', class: "form-container__label-title" %>
    <%= form.text_field :thread_title,  class: "form-container__input-title" %>
  </div>

  <div class="actions">
    <%= form.submit "Create Thread", class: "action-button" %>
  </div>
<% end %>
